이 채팅모듈 사용법은
메시지를 보내려는 측에서 server.h에 포함된 server함수를 호출한 후
메시지 하나를 입력하고, 메시지를 받는측에서 client.h에 포함된 client함수를 호출하면되요.
함수를 호출하는 방법은
두 함수 모두
server(port);
client(port);
이고 port에는 네자리 숫자 아무거나 들어가면 되고 메시지를 주는 쪽과 받는 쪽 포트는 무조건 같아야 해요.
